 <Card
                  className={`${glassy} min-w-[180px] max-w-[180px] cursor-pointer hover:scale-[1.03] transition-transform`}
                  onClick={() => setSelectedBook(book)}
                >
                  <CardContent className="p-2 flex flex-col items-center">
                    <img
                      src={
                        book.volumeInfo.imageLinks?.thumbnail ||
                        "/placeholder.png"
                      }
                      alt={book.volumeInfo.title}
                      className="w-full h-44 object-cover rounded-xl border mb-2"
                    />
                    <div className="text-base font-semibold text-center line-clamp-2">
                      {book.volumeInfo.title}
                    </div>
                    <div className="text-xs text-muted-foreground mt-1">
                      {book.volumeInfo.authors?.join(", ")}
                    </div>
                    <div className="flex gap-2 mt-2">
                      <Button
                        variant={
                          favorites.find((b) => b.id === book.id)
                            ? "secondary"
                            : "outline"
                        }
                        size="icon"
                        onClick={(e) => {
                          e.stopPropagation();
                          e.preventDefault();
                          toggleFavorite(book);
                        }}
                        className="rounded-full"
                        tabIndex={-1}
                      >
                        <Heart
                          className={`w-5 h-5 transition-colors ${
                            favorites.find((b) => b.id === book.id)
                              ? "fill-red-500 text-red-500"
                              : ""
                          }`}
                        />
                      </Button>
                      <Button
                        variant={
                          currentlyReading.find((b) => b.id === book.id)
                            ? "secondary"
                            : "outline"
                        }
                        size="icon"
                        onClick={(e) => {
                          e.stopPropagation();
                          e.preventDefault();
                          toggleCurrentlyReading(book);
                        }}
                        className="rounded-full"
                        tabIndex={-1}
                      >
                        <BookOpen
                          className={`w-5 h-5 transition-colors ${
                            currentlyReading.find((b) => b.id === book.id)
                              ? "fill-blue-500 text-blue-500"
                              : ""
                          }`}
                        />
                      </Button>
                    </div>
                  </CardContent>
                </Card>